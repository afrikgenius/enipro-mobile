package com.enipro.services;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class SessionScheduleService extends BroadcastReceiver {

    @Override
    public void onReceive(Context context, Intent intent) {
        // Check the user type and look for sessions in database and fire notification if a session is about to start

    }
}
