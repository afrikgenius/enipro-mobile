<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/search_item"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_marginTop="7dp"
    android:background="?attr/selectableItemBackground">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/message_search_avatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="12dp" />

    <TextView
        android:id="@+id/message_search_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="12dp"
        android:layout_toEndOf="@id/message_search_avatar"
        android:textSize="12sp"
        tools:text="@string/user_post_mock_first_name" />

    <ImageView
        android:id="@+id/hyphen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/message_search_name"
        android:background="@null"
        android:src="@drawable/ic__" />

    <TextView
        android:id="@+id/message_search_headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginEnd="12dp"
        android:layout_toEndOf="@id/hyphen"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="12sp"
        tools:text="@string/search_headline" />

</RelativeLayout>